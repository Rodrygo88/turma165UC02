import style from "./Header.module.css";

export default function Header({ }) {
    return (
        <>
            <header className={style.topo}>
                <h1>Site React</h1>
            </header>

        </>
    )
}